apiVersion: v1
kind: Service
metadata:
  name: {{ include "alethia-starter.fullname" . }}
  labels:
    {{- include "alethia-starter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alethia-starter.selectorLabels" . | nindent 4 }}
